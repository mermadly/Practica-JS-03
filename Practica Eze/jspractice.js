// alfanumérico y guiones bajos y $
// empieza con una letra o con un guion bajo, no puede comenzar con un número

var nombre;

// operador de asignación: me permite guardar algo en la var. el op es el =

nombre = 'Victoria';
//  en la misma línea podemos crear una variable y asignarle un valor

var apellido = 'Echavarría';

// tipos de datos "básicos" o "primitivos"
//strings, númericos, booleanos

// strings: siempre van entre comillas y puede tener cualqueir tipo de caracter

var mensaje ='4387538947598345 JDKFJSD FFkkmmd 4·)%/·(%=%/';

// concatenar strings con +
var nombreCompleto = nombre + ' ' + apellido;

// númericos: van sin comillas 

var unNumero = 9;
var otroNumero = 29;
var algunNumeroNegativo = -5;

// los decimales se escriben con punto (.) en vez de coma (,)

var algunNumeroConDecimales = 20.5;

var resultado = unNumero + otroNumero;
var resultadoResta = unNumero - otroNumero;
var resultadoMultiplicacion = unNumero * otroNumero;
var resultadoDivision = unNumero / otroNumero;

var base = 12
var altura = 15

var areaTriangulo = base * altura;

// booleanos: van a tener solo dos valores posibles "true" o "false", se escriben sin comillas

var unBooleano = true;
var elOtroBooleano = false; 

// undefined: una variable que no está definida

var variableUndefined;

// null: una variable no tiene un valor

var unaVariableconNull = null;

var numero = 10;

numero = numero + 1;
numero++;

numero = numero -1;
numero --;
